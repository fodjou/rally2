<?php



if (!function_exists('formatTotalTime')) {
    function formatTotalTime($time)
    {
        // Logique de formatage du temps
        // Par exemple :
        return date('H:i:s', $time);
    }

}
